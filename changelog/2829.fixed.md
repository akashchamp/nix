Fixed `AlgAddr::new` to reject `alg_type`/`alg_name` values that would
leave no room for a NUL terminator in the underlying fixed-size
buffers, preventing an out-of-bounds read in
`AlgAddr::alg_type`/`AlgAddr::alg_name`. `AlgAddr::new` now returns
`Result<AlgAddr>` instead of `AlgAddr`.
